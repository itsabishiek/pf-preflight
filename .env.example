# Keep this server-only. Never prefix it with NEXT_PUBLIC_.
GEMINI_API_KEY=your_google_ai_studio_key

# Optional: defaults to the fast classification model below.
# GEMINI_MODEL=gemini-3.5-flash-lite
